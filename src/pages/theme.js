import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#7C4DFF',
      extra: '#B388FF',
    },
    type: 'dark',
    background: {
      default: '#0F0529',
    },
  },
});

export default theme;
