import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0F0529',
    },
    type: 'dark',
    background: {
      default: '#0F0529',
    },
  },
});

export default theme;
